# 耳边前端环境变量
#
# 若未设置 VITE_API_BASE_URL，前端全部走内置 mock 数据（本地开发 / 离线预览友好）。
# 设置后，前端会从 Directus 后台拉真数据；失败时自动回退到 mock，保证可用性。

# 生产：填 Railway 上 Directus 的域名
# VITE_API_BASE_URL=https://erbian-admin-production.up.railway.app

# 本地：docker compose 起的 Directus
# VITE_API_BASE_URL=http://localhost:8055

# VPS 部署（主机 nginx 把 /api/ 反代到 directus 容器 127.0.0.1:8055）：
# VITE_API_BASE_URL=/api
#
# GitHub Pages 演示版（无后端）：留空即可，数据走 mockData.ts。
